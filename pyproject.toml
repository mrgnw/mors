[project]
name = "calendars"
version = "0.1.0"
description = "Gym class calendar server"
requires-python = ">=3.13"
dependencies = [
	"fastapi",
	"uvicorn",
	"icalendar",
	"playwright",
	"python-dotenv",
]
